import React from 'react';

export const Form = () => {
  return (
    <>
      <div>Form</div>
      <form>
        <input type="text" />
        <select>Type car model</select>
        <select>Type car model</select>
        <select>Type car model</select>
        <select>Type car model</select>
        <select>Type car model</select>
        <button type="submit">Search</button>
      </form>
    </>
  );
};
